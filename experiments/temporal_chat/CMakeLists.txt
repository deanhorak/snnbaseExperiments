add_executable(temporal_chat main.cpp)
target_link_libraries(temporal_chat PRIVATE snnbase::snnbase)
target_compile_features(temporal_chat PRIVATE cxx_std_20)
set_target_properties(temporal_chat PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}")
if(MSVC)
  target_compile_options(temporal_chat PRIVATE /W4 /WX)
else()
  target_compile_options(temporal_chat PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()
if(BUILD_TESTING)
  add_executable(temporal_chat_tests "${PROJECT_SOURCE_DIR}/tests/temporal_chat_tests.cpp")
  target_include_directories(temporal_chat_tests PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
  target_link_libraries(temporal_chat_tests PRIVATE snnbase::snnbase)
  target_compile_features(temporal_chat_tests PRIVATE cxx_std_20)
  if(MSVC)
    target_compile_options(temporal_chat_tests PRIVATE /W4 /WX)
  else()
    target_compile_options(temporal_chat_tests PRIVATE -Wall -Wextra -Wpedantic -Werror)
  endif()
  add_test(NAME temporal_chat_learning COMMAND temporal_chat_tests)
  set_tests_properties(temporal_chat_learning PROPERTIES LABELS "temporal-chat;cpu;learning" TIMEOUT 120)
  find_package(Python3 COMPONENTS Interpreter QUIET)
  if(Python3_Interpreter_FOUND)
    add_test(NAME temporal_chat_protocol
      COMMAND "${CMAKE_COMMAND}" -E env "TEMPORAL_CHAT_CORE=$<TARGET_FILE:temporal_chat>"
      "${Python3_EXECUTABLE}" "${PROJECT_SOURCE_DIR}/tests/python/test_temporal_chat.py")
    set_tests_properties(temporal_chat_protocol PROPERTIES LABELS "temporal-chat;cpu;protocol" TIMEOUT 120)
  endif()
endif()
